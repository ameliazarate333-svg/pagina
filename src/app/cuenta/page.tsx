import Dashboard from "@/components/Dashboard";

export const metadata = { title: "Mi Cuenta · AZ — Amelia Zárate", robots: { index: false, follow: false } };

export default function CuentaPage() {
  return <Dashboard />;
}
